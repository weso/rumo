[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "rumo"
version = "0.1"
requires-python = ">=3.8"
dependencies = ["polars>=0.20"]

[tool.maturin]
features = ["python"]
module-name = "rumo"
